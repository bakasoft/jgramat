package gramat.expressions.values;

import gramat.expressions.Expression;
import gramat.util.parsing.Location;

abstract public class DataExpr extends Expression {

    public DataExpr(Location location) {
        super(location);
    }

}
